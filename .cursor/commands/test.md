Run the `test-runner` subagent after the latest code changes.

It must:
1. Identify what changed.
2. Run the relevant test suite.
3. Report failures with file and line.
4. Propose minimal fixes only (no refactor of passing code).
