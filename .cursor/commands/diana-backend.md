Invoke the `backend-agent` subagent for this task.

Scope:
- Backend/API work only.
- Files under `/agents`, `/api`, `/routers`, `/services`, `/models`.

Requirements:
- Use async FastAPI patterns and Pydantic schemas.
- Ensure routes are user_id scoped.
- Do not modify frontend files.
