Invoke the `frontend-agent` subagent for this task.

Scope:
- UI/frontend work only.
- Files under `/src` or `/frontend`.

Requirements:
- Use typed React/TypeScript components and props interfaces.
- Use Tailwind styling and service-based data access patterns.
- Do not modify backend files.
